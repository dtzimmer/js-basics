// Implement this isLandscape function that returns true or false
//  depending on if width is greater than the height.

console.log(isLandscape(800, 600));

function isLandscape(width, height){
    return (width > height);  // there is no need to explicitly return true or false e.g. ? true : false;
}